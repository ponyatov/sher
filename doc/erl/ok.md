# `ok`

In Erlang, `ok` is an **[[erl/atom]]** that is commonly used in function return values to indicate successful execution, often paired with other data using a tuple, or used alone when no additional data needs to be returned.
