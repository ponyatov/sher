# What you need to dive in

- a text editor ([[vscode/VSCode|VSCode]], and the 
- Erlang environment

```sh
sudo apt-get install -uy erlang rebar3
```

- start [[erl/erl|erl]] to get REPL
- run `halt().` to exit [[erl/halt|halt]]()
	- also Ctrl+C Ctrl+D keys

