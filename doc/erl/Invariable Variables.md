# Invariable Variables

immutable & single assignment !

you won't go far without being able to store results somewhere.

- names starts with `[A-Z]`
- you can assign a value to a variable exactly once
- `=` pattern matching
- `f(Var)` = forget variable (only work in the shell)
	- `f()` = forget all variables
